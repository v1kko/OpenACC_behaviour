name: Github Pages

on:
  push:
    branches:
      - main

jobs:
  publish:
    name: Publish github pages
    runs-on: ubuntu-latest
    steps:
      - name: Deploy MkDocs
        uses: mhausenblas/mkdocs-deploy-gh-pages@1.26
